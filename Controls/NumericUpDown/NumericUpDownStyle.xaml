<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:LenWeaver.Utilities">
    
    <Style                                              TargetType="{x:Type local:NumericUpDown}">
        <Setter                                         Property="Template">
            <Setter.Value>
                <ControlTemplate                        TargetType="{x:Type local:NumericUpDown}">
                    <ControlTemplate.Resources>
                        <Style                          x:Key="NumericUpDownTextBoxStyle" TargetType="TextBox">
                            <Setter                     Property="Foreground" 
                                                        Value="{Binding PositiveForeground, RelativeSource={RelativeSource TemplatedParent}}" />

                            <Style.Triggers>
                                <DataTrigger            Binding="{Binding IsNegative, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                    <Setter             Property="Foreground" 
                                                        Value="{Binding NegativeForeground, RelativeSource={RelativeSource TemplatedParent}}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ControlTemplate.Resources>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition           Width="Auto"    MinWidth="24"   />
                            <ColumnDefinition           Width="*"       MinWidth="75"   />
                            <ColumnDefinition           Width="Auto"    MinWidth="24"   />
                        </Grid.ColumnDefinitions>
                        
                        <RepeatButton                   x:Name="PART_DecreaseButton"    Grid.Column="0"
                                                        Height="{Binding ActualHeight, ElementName=PART_TextBox}"
                                                        Delay="{TemplateBinding InitialRepeatDelay}"
                                                        Interval="{TemplateBinding RepeatIntervalDelay}"
                                                        HorizontalContentAlignment="Center"
                                                        VerticalContentAlignment="Center"
                                                        Width="{TemplateBinding MinimumButtonWidth}">
                            <local:GeometryPresenter    x:Name="gpDecreasePath"
                                                        FillBrush="Black"
                                                        PathMarkup="M42,73 17,30 70,30 Z"
                                                        BoundsOverride="0 0 100 100"
                                                        Height="{Binding ActualHeight, ElementName=PART_TextBox}"
                                                        Width="{TemplateBinding MinimumButtonWidth}" />
                        </RepeatButton>
                        
                        <TextBox                        x:Name="PART_TextBox"           Grid.Column="1"
                                                        Focusable="true"
                                                        IsReadOnly="true"
                                                        Padding="2"
                                                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                        VerticalContentAlignment="Center"
                                                        Style="{StaticResource NumericUpDownTextBoxStyle}" />
                        
                        <RepeatButton                   x:Name="PART_IncreaseButton"    Grid.Column="2"
                                                        Delay="{TemplateBinding InitialRepeatDelay}"
                                                        Interval="{TemplateBinding RepeatIntervalDelay}"
                                                        Height="{Binding ActualHeight, ElementName=PART_TextBox}"
                                                        HorizontalContentAlignment="Center"
                                                        VerticalContentAlignment="Center"
                                                        Width="{TemplateBinding MinimumButtonWidth}">
                            <local:GeometryPresenter    x:Name="gpIncreasePath"
                                                        FillBrush="Black"
                                                        PathMarkup="M42,17 17,60 70,60 Z"
                                                        BoundsOverride="0 0 100 100"
                                                        Height="{Binding ActualHeight, ElementName=PART_TextBox}"
                                                        Width="{TemplateBinding MinimumButtonWidth}" />
                        </RepeatButton>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>