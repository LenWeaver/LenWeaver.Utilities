From xaml:        <SymbolView ImageUri="/LenWeaver.Utilities;component/Images/Database-Connect.png">
From code behind: svSymbolImage.ImageUri = new Uri( "/LenWeaver.Utilities;component/Images/Folder-01-Star.png", UriKind.Relative );
Icon                = new BitmapImage( new Uri( "pack://application:,,,/LenWeaver.Utilities;component/Images/Folder-01-Star.png", UriKind.RelativeOrAbsolute ) );
Icon = ImageExtensions.CreateBitmapImage( "pack://application:,,,/LenWeaver.Utilities;component/Images/Database-Connect.png", 64, 64 );
Icon="/LenWeaver.Utilities;component/Images/Database-Connect.png"

xmlns:sys="clr-namespace:System;assembly=mscorlib"
xmlns:lw="clr-namespace:LenWeaver.Utilities;assembly=LenWeaver.Utilities"
xmlns:lwui="clr-namespace:LenWeaver.UI.WPF;assembly=LenWeaver.UI.WPF"
xmlns:lwdb="clr-namespace:LenWeaver.Data;assembly=LenWeaver.Data"
xmlns:lwdbc="clr-namespace:LenWeaver.Data.Common;assembly=LenWeaver.Data"

Binding:

FontFamily="{Binding Path=(lw:FontExtensions.GlobalMonospace)}"
FontFamily="{x:Static lw:FontExtensions.GlobalMonospace}"
<TextBlock Text="{Binding ElementName=txtValue, Path=Text, Converter={StaticResource YesNoToBooleanConverter}}"></TextBlock>
IsEnabled="{Binding ElementName=txtSql, Path=Text, Converter={StaticResource lw:StringLengthToBoolean}}">

<Style TargetType="{x:Type local:DatabaseTreeViewItem}">
    <Setter Property="ContextMenu" Value="{StaticResource ctxDatabase}" />
</Style>

<interactivity:CallMethodAction MethodName="IncrementCount"
                                        TargetObject="{x:Bind}" />

<Window.Resources>
    <sys:Double x:Key="AboutFontSize">18</sys:Double>
    <sys:Double x:Key="ProductNameFontSize">22</sys:Double>
</Window.Resources>

FontSize="{DynamicResource ProductNameFontSize}"

<Application.Resources>
    <ResourceDictionary>
        <ResourceDictionary.MergedDictionaries>

            <!-- 1. Constants, colors, brushes -->
            <ResourceDictionary Source="Resources/Colors.xaml" />

            <!-- 2. Styles that depend on colors -->
            <ResourceDictionary Source="Resources/Styles.xaml" />

            <!-- 3. Control templates that depend on styles -->
            <ResourceDictionary Source="Resources/Templates.xaml" />

            <!-- 4. Menus, context menus, icons -->
            <ResourceDictionary Source="Resources/Menus.xaml" />

            <!-- 5. TreeView styles (depends on menus) -->
            <ResourceDictionary Source="Resources/TreeViewStyles.xaml" />

        </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
</Application.Resources>

int[] someArray = new int[5] { 1, 2, 3, 4, 5 };
int[] subArray1 = someArray[0..2]; // { 1, 2 }
int[] subArray2 = someArray[1..^0]; // { 2, 3, 4, 5 }